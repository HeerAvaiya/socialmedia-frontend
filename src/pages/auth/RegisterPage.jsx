import React from 'react';
import Register from '../../views/auth/Register';

const RegisterPage = () => {
    return (
        <React.Fragment>
            <Register />
        </React.Fragment>
    );
};

export default RegisterPage;
