import React from 'react';
import Home from '../views/Home';

const HomePage = () => {
    return (
        <React.Fragment>
            <Home />
        </React.Fragment>
    );
};

export default HomePage;